# Arco Design Pro 模板依赖 @arco-plugins/vite-plugin-svgr 已停更(最新 0.7.2,
# 5 年前发布),其 peerDependencies 声明仅支持 vite ^2.6.0,但实测在 vite 5 下正常工作。
# npm ci 默认严格校验 peer 依赖,会因此 ERESOLVE 失败。
# 此文件使本地与 CI 安装行为一致:放宽 peer 校验(等同于 npm install --legacy-peer-deps)。
legacy-peer-deps=true
